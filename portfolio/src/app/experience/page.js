import Experience from "@/components/Experience";
import React from "react";

const page = () => {
	return (
		<main className="border w-full">
			<Experience />
		</main>
	);
};

export default page;
